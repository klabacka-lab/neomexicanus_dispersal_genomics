May 28, 2026
	- Perry and Randy met to discuss project. Created github repository for code. Perry is going to try to convert the unasigned .bam file for 7888 to fastq files (which he thinks he already did, and the file might not have good reads. This could be why the old mapped file seem truncated)
	- Started the process of converting 7888's unassigned bam to a fastq file. Using samtools bamtofastq tool.

May 30, 2026
	- With the 7888 fastq file complete I started the xengsort tool to classify it. Used the standard of k = 25 to match the other classified files that were chosen for use downstream.

June 4, 2026
	- Updated Snakefile to match new location and checked to make sure all jobs would still say complete. Removed unecessary copies from my nobackup dir.
	- created a script to index the reference fastas for use in variant calling

June 5, 2026
	- created new snakefile job to run the deepvariant tool for variant calling using the pacbio preset.
	- ran into docker errors but solved after reading documentation on the byu office of research computing

June 9, 2026
	- Perry and Randy met to discuss variant filtration. Here are some thoughts:
		1. Remove low-quality variants
			bcftools view -O z -o filtered.vcf.gz -e 'QUAL<=20' in.vcf.gz
			# https://www.htslib.org/workflow/filter.html
		2. Remove low-quality genotypes
			vcftools --vcf <name_of_vcf>.vcf --out <name_of_new_vcf>_HardFilterStep1 --minGQ 20 --recode --recode-INFO-all
			mv <name_of_new_vcf>_HardFilterStep1.recode.vcf <name_of_new_vcf>_HardFilterStep1.vcf
			# Check if this removes the variants or just converts the genotypes to './.' genotypes
		3. Remove './.' genotypes
		4. Get rid of low-depth variants
			vcftools --vcf "$2"_HardFilterStep1.vcf --out "$2"_HardFilterStep2 --minDP 10 --recode --recode-INFO-all
	- After filtration, we need to look at all vcfs and identify the Variant Sites
	- Use those combined Variant Sites to shrink the .bam files OR see if there is a way for deepvariant to take specific loci
	- Re-call variants from shrunken .bam files using deepvariant, but this time generate gvcfs

	- created new rules for each filter step in the Snakefile. Each step has also been designed to produce a new index file. This should result in a new vcf file for each filter step for both the host and graft files in each sample.

June 11, 2026
	- added new rule to generate a bed file for both the graft and host vcfs that contains all loci of interest.
	-adding rule to re-call the variants using the specified locis found in the bed files.

June 18, 2026
	- met to discuss how to move forward with newly generated gvcf files
	- created rules to merge the gvcf for each sample. need to check to make sure it still gives 0/0 or ./. at locations where the snp is unique to a few individuals.
